


function cars(manufacturer: string ,model: string, feature: string, value: string) : void{
    let car ={
        manufacturer:manufacturer,
        model:model,
        feature:feature,
        value:value,
    }
    console.log(car);
}

cars("Toyota", "corolla", "electric", "black");
