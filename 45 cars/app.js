function cars(manufacturer, model, feature, value) {
    var car = {
        manufacturer: manufacturer,
        model: model,
        feature: feature,
        value: value,
    };
    console.log(car);
}
cars("Toyota", "corolla", "electric", "black");
